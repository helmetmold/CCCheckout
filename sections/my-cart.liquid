<div>
    <h2> Cart </h2>
    <div claa="my-cart_items">
        {% for item in cart.items %}
            </div>
                <a href="{{item.url}}">
                    {{item.title}}
                </a>
            </div>
            <div>
                Price: {{item.final_price | money}}
            </div>
            <div>
                Quantity: 
                <a href="">
                    Minus
                </a>
                {{item.quantity}}
                <a href="">
                    Plus
                </a>
            </div>
        {% endfor %%}
    </div>
</div>